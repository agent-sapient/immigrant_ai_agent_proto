# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: BrowserUseAIAgent
    description: BrowserUseAIAgent
    version: "1.0"
servers:
    - url: http://101.6.161.45:18000
      description: server
paths:
    /api/batch_use_ai_agent:
        post:
            tags:
                - BrowserUseAIAgent
            operationId: BrowserUseAIAgent_BatchUseAIAgent
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/BatchUseAIAgentRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/BatchUseAIAgentResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/use_ai_agent:
        post:
            tags:
                - BrowserUseAIAgent
            operationId: BrowserUseAIAgent_UseAIAgent
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UseAIAgentRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UseAIAgentResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        BatchUseAIAgentRequest:
            required:
                - prompt
                - models
            type: object
            properties:
                prompt:
                    type: string
                models:
                    type: array
                    items:
                        type: integer
                        format: enum
            description: 批量请求消息
        BatchUseAIAgentResponse:
            type: object
            properties:
                results:
                    type: array
                    items:
                        $ref: '#/components/schemas/ModelResult'
            description: 批量响应消息
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ModelResult:
            type: object
            properties:
                model:
                    type: integer
                    format: enum
                result:
                    type: string
                outputFileUrls:
                    type: array
                    items:
                        type: string
            description: 单个模型的结果
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        UseAIAgentRequest:
            required:
                - prompt
            type: object
            properties:
                prompt:
                    type: string
        UseAIAgentResponse:
            type: object
            properties:
                result:
                    type: string
                outputFileUrls:
                    type: array
                    items:
                        type: string
    securitySchemes:
        BasicAuth:
            type: http
            scheme: basic
tags:
    - name: BrowserUseAIAgent
